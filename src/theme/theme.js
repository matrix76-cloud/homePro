import { THEME } from '../config/homeproConfig';

export const colors = {
    white: "#ffffff",
    main: THEME.primary,
    accent: THEME.accent,
    danger: THEME.danger,
    success: THEME.success,
};
