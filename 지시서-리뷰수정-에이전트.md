# [리뷰수정 에이전트] 상주 지시서

지휘: 카스(메인 터미널). 이 지시서를 받은 터미널 에이전트는 아래 루프로 상주한다.

## 목표
리뷰 허브(/review)에 형이 올린 미답변 지시글을 확인 → 코드 수정 → [처리완료] 답글. 이걸 반복한다.

## 환경
- 프로젝트: /Users/a1111/Downloads/2026Dev/mainproject/2026Web/homePro
- dev 서버: http://localhost:3002 (이미 떠 있음 — 새로 띄우지 말 것. 죽어 있으면 지휘에게 보고)
- 리뷰 허브: http://localhost:3002/review

## 작업 루프 (한 사이클)
1. 미답변 확인:
   `node dump-reviews.mjs /tmp/reviews.json`
   → 출력의 "unanswered roots" 목록이 처리 대상. 상세 내용은 /tmp/reviews.json 에서 pid 로 찾아 전문(text) 확인.
2. 지시글마다: 해당 화면/기능 코드를 찾아 수정. 확인이 필요하면 puppeteer 로 3002 화면 띄워 검증.
   - puppeteer 로드(프로젝트에 미설치):
     ```js
     const { createRequire } = await import("module");
     const require = createRequire("/Users/a1111/.npm/_npx/55158e48eb5c59f7/");
     const puppeteer = require("puppeteer");
     const browser = await puppeteer.launch({ executablePath: "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome", headless: "new", args: ["--no-sandbox"] });
     ```
   - 테스트 로그인: http://localhost:3002/seed-login (계정 버튼 클릭)
3. 답글 등록: replies.json 을 만들어
   `node post-review-replies.mjs replies.json`
   - 형식: `[{ "replyTo": "<지시글 pid>", "screenId": "<지시글 screenId>", "text": "[처리완료] 무엇을 어떻게 고쳤는지 2~3줄" }]`
   - 처리 못 했으면 [대기] 또는 [미처리] + 사유.
4. 사이클 끝나면 처리 내역을 한 줄씩 요약 보고하고 대기. 사용자가 "확인해"라고 치면 1번부터 다시.

## 작업 파일 (소유권)
- 수정 허용: src/ 아래 화면·기능 코드 전반.
- 보고만(수정 금지): src/service/*, src/config/*, src/context/*, App.js, functions/* — 공용 파일은 수정안을 답글/보고에 적고 지휘가 반영.
- 절대 건드리지 마: public/qa/**, scripts/qa-*, qa-지시서/**, src/dev/** — 테스트 에이전트 소유.

## 금지
- git commit / push 금지 (지휘가 검수 후 커밋)
- firebase deploy 금지
- Firestore 기존 데이터 수정·삭제 금지 (리뷰 답글 addDoc 만 허용)

## 스타일 (수정 시 필수)
- 이모지 금지. 좌측 accent 테두리 바(border-left 강조선) 금지. 상태 뱃지(연배경+진글씨 pill) 신규 금지.
- 보라색 금지 — 포인트 컬러는 프로젝트 현행(브랜드 컬러) 유지.
- 글씨 하한: 본문 15px, 보조 13px, 라벨·버튼 14px.
- 탭/상태 전환 시 레이아웃 안 흔들리게 min-height + 내부 스크롤.

## 완료 조건
미답변 0 상태 유지. 매 사이클 종료 시 "처리 N건 / 대기 M건 (사유)" 형식으로 보고.
