node_modules/

# 빌드 산출물
build/
.firebase/

# 환경변수 — 키가 들어있으므로 커밋 금지 (.env.example 만 커밋)
.env
.env.local
.env.production
.env.client
functions/.env
functions/.env.*
!functions/.env.example

# 서비스 계정 키 — 절대 커밋 금지
*serviceAccountKey*.json
*-firebase-adminsdk-*.json
_admin.mjs

# 이관 작업 산출물(사용자 데이터·해시 포함)
_migration/

# 리뷰 덤프 산출물(생성물)
reviews.json
.env.development.local
.env.bak.*
